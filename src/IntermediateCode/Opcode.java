package IntermediateCode;

public enum Opcode {
    ADD, SUB, MULT, DIV, MOD,
    AND, OR, NOT,
    SKIP, GOTO,
    GT, LT, GE, LE, EQ, NE,
    CALL, FUN, RET, PARAM,
    ASSIGN, PMB,
    OUTPUT, OUTPUTLN, READ_STRING, READ_INT,
    MEMINIT
}
